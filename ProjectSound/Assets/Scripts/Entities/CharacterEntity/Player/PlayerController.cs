using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerController : MonoBehaviour
{
    //Controla cuando se ha de saltar
    bool jump;
    //Cantidad de movimiento
    float move;
    //Instancia del joystick
    //Instancia del botón de salto
    //Instancia del botón de acción

    //Intancia del comportamiento del personaje
    public PlayerBehaviour behaviour;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void FixedUpdate()
    {
        
    }
    /* Método para controlar los inputs en PC */
    void inputsPc() { }

    /* Método para controlar los input en móvil */
    void inputsHandheldDevice() { }

}
